# Wake-Up Report — 2026-05-17

**Summary:** Merged 4 PRs (#319, #320, #321, #322), 0 in review, 3 queued, 5+ blocked on Colin.

---

## Merged This Session

| PR | Task | What it does | F19 delta |
|----|------|-------------|-----------|
| #319 | B6 — MCP server registration | `.mcp.json` at repo root so Claude Code discovers `mcp-server/index.mjs` automatically; fixes `tasks.mjs` status enum + column names | 100% net-new (MCP was non-functional before) |
| #320 | A5 — AI Control Center | `/ai-control` cockpit page — 4-tab shell (Audit Trail, Config, Windows, Capabilities) reading `agent_events`, `harness_config`, `session_beacons`, `capability_registry`; nav link in sidebar | 100% net-new |
| #321 | A2 — Permit Pre-Screener | `/permit` cockpit page — Edmonton SODA dataset `q7d6-ambg` lookup by house number + street; property card + permit guidance by tax class; nav link in Household sidebar | 100% net-new |
| #322 | E3 — Grocery Receipt Scan | `POST /api/diet/receipts/scan` calls Claude Haiku Vision, extracts grocery items + estimated nutrition; `ScanReceiptButton.tsx` review table with Add All; mounted on /diet Receipts tab | >100% vs manual entry: 0 to ~15 items/min, nutrition fields now populated |

**Sprint 7 Chunk F:** No PR needed. All Chunk F content (`BalanceSheetPage.tsx`, `net-worth-sync` cron, currency fields) already present in main from prior PRs (#274 etc.). Temp branch `temp-chunk-f-rebase` was rebased clean and dropped — empty after rebase confirms prior work was complete.

---

## Task Queue

| Status | Count | Delta |
|--------|-------|-------|
| completed | 113 | +10 vs session start (103) |
| cancelled | 42 | — |
| dismissed | 4 | — |
| queued | 3 | -4 (B6, A5, A2, E3 completed) |
| awaiting_approval | 1 | — |
| awaiting_grounding | 1 | — |

---

## Queued for Next Cycle (buildable without Colin)

None remaining — all 3 queued tasks are blocked on Colin (see below).

---

## Blocked on Colin — Action Required

| Task | What's needed |
|------|--------------|
| **C2** — Statement Coverage Grid v2 | `sender_domains` list for 7 business accounts (which Gmail senders = which accounts). Without this the Gmail coverage scan has no target to match against. |
| **B5** — Square webhook ingestion | `SQUARE_WEBHOOK_SIGNATURE_KEY` in Vercel env + Square Dashboard webhook endpoint registration at `lepios-one.vercel.app/api/square/webhook`. |
| **E6** — Megan reminder inbox | Telegram bot decision: should Megan messages go to the existing `loeppky_daily_bot` or a new Megan-specific bot? Need bot token before the route can be wired. |
| **diet-inventory-three-bucket-summary** | `awaiting_grounding` — Colin needs to verify the three bucket definitions on `/diet` Inventory tab before the summary widget is built (bucket boundaries). |
| **C4** — Tesla auto-valuation | PR #303 open, `awaiting_approval`. Colin needs to review and approve/reject before it can merge. |

---

## Sprints 8–10 — Not Started, Needs Planning Pass

- **Sprint 8 (BR Tier 3):** Business Review deep-drill enhancements. Not in `task_queue`. Requires Streamlit study + coordinator planning session before builder can touch it.
- **Sprint 9 (Shipment Manager):** Amazon FBA shipment tracking, multi-chunk. Not in `task_queue`.
- **Sprint 10 (Reporting):** Cross-module reporting layer. Not in `task_queue`.

These can't proceed autonomously — they need a scope session first.

---

## Housekeeping

- **Stale worktree directories** (git-pruned but physical dir remains due to Windows file locks): `lepios-a2-permit`, `lepios-e3-scan`. Safe to `rm -rf` both — branches deleted from origin, git metadata pruned.
- **`lepios-a5-ai-control` worktree**: still listed in `git worktree list` (from the A5 build earlier this session). Safe to remove if it's no longer in use.
- **`lepios-b5` worktree**: still registered — this was from the B5 Square webhook session that was blocked. Leave it or remove if B5 is deferred further.

---

## Surprises / Near-Misses

- **Sprint 7 Chunk F was already done.** The rebase produced an empty commit — all BalanceSheet/NetWorth/sync content was already in main from prior sprint work. Saved ~30 min of unnecessary PR work by detecting this before pushing.
- **Supabase `result` column is JSON type.** Plain string `UPDATE ... result = 'text'` errors as invalid JSON. Must use `'{"key":"val"}'::json`. Updated the task-complete SQL pattern accordingly.
- **`output_summary` column doesn't exist.** The `task_queue` table uses `result` (JSON) not `output_summary` (string). Correct column name verified before each UPDATE.
- **A2 task ID is a UUID**, not a string literal `'a2'`. Always query by `task ILIKE '%A2%'` first to get the UUID, then UPDATE by UUID.
