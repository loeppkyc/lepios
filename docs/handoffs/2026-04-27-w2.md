# W2 Handoff — 2026-04-27

**Window:** W2 (smoke test framework)
**Status:** Idle — stood down due to Vercel deploy stall
**Written:** 2026-04-27

---

## Last completed

**route-health smoke test module** — commit `13bfb44`, merged to main.

- `lib/harness/smoke-tests/route-health.ts` — exports `runRouteHealthSmoke(baseUrl, commitSha?)`
- Checks 3 routes in parallel with 10s timeout: `GET /api/health` (200), `POST /api/twin/ask` (200), `GET /api/telegram/webhook` (405)
- On failure: inserts `agent_events` (`smoke_test_failed`) + `outbound_notifications` Telegram alert (`correlation_id: smoke-fail-{sha8}`) + `task_queue` P1 row
- On pass: inserts `agent_events` (`smoke_test_passed`) with all route latencies
- `chat_id` from `harness_config` at call time
- 25 tests, all passing. Suite: 1048 passing, 0 failing.

**Module is standalone — not yet wired into any trigger.**

---

## Resume condition

Both of these must be true before resuming:

1. **W4 unblocks production deploy** — current Vercel deploy stall resolved, main is deploying clean
2. **W1 ships cron-registration module** — `lib/harness/smoke-tests/cron-registration.ts` exists and is merged

---

## Next task when resumed

**Deploy-gate integration** — wire both smoke modules into `deploy-gate-runner`.

Per scope doc `docs/harness/deploy-smoke-tests-scope.md`:

1. In `lib/harness/deploy-gate.ts` — after `mergeToMain()` succeeds, write `agent_events` row `action='production_smoke_pending'` with `meta: { merge_sha, branch, commit_sha, merged_at }`
2. In `app/api/cron/deploy-gate-runner/route.ts` — add query for `production_smoke_pending` events (same pattern as existing `deploy_gate_triggered` query), call `runRouteHealthSmoke` + `runCronRegistrationSmoke` in parallel
3. Dedup guard: check for existing `production_smoke_complete` row with same `merge_sha` before running
4. Write `production_smoke_complete` event on finish (pass or fail)
5. Add smoke stats line to `lib/orchestrator/digest.ts` via `buildDeploySmokeStatsLine()`
6. Update `deploy-gate.test.ts` with integration tests for the new trigger path

Files to touch: `lib/harness/deploy-gate.ts`, `app/api/cron/deploy-gate-runner/route.ts`, `lib/orchestrator/digest.ts`, `tests/harness/deploy-gate.test.ts`.
**Do not touch** `lib/harness/smoke-tests/route-health.ts` or `cron-registration.ts` — those are done.

---

## Key context

- `route-health.ts` and (once W1 ships) `cron-registration.ts` are both in `lib/harness/smoke-tests/` — no barrel/index file, no shared state, each is independently callable
- Scope doc is at `docs/harness/deploy-smoke-tests-scope.md` (commit `9a4a255`) — read §2 Design and §4 Files Expected to Change before starting integration
- `LEPIOS_BASE_URL` in `harness_config` is the base URL for all smoke self-calls (same pattern as drain self-trigger)
- The scope doc header still says "Awaiting Colin approval" — this can be updated to "Approved 2026-04-27" when W4 unblocks and work resumes
